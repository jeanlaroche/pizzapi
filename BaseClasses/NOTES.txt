
Find an element by id:
var src = document.getElementById("x");

Remove childs of a node.
while (src.hasChildNodes()) {
	src.removeChild(src.lastChild);}

// Confirmation panel;	
result = confirm("You Sure?");
// Print to log:
console.log(result);

Loop around array:
var arrayLength = data.image.length;
for (var i = 0; i < arrayLength; i++) {
	//console.log(data.image[i]);
	
	// Create an element.
	img = document.createElement("img");
	img.src = data.host+data.image[i];
	// Add it as a child.
	src.appendChild(img);
	// This is for a text
	var txt = document.createElement("P"); 
	txt.innerText = data.image[i]+" ^^"; 
	src.appendChild(txt);
	// This is for a button
	var button = document.createElement("button");
	button.innerHTML = "Delete ";
	button.image=data.image[i];
	// This is to add a listener to the button.
	button.addEventListener ("click", function(event) {
		//console.log(this.image);
		result = confirm("You Sure?");
		console.log(result);
		if(result) deleteImage(this.image);
		});
	src.appendChild(button);

	var hr = document.createElement('hr');
	src.appendChild(hr);				
}
